{
  "scripts": {
    "heroku-prebuild": "npm install --prefix ./project/spa/static/spa && npm run build --prefix ./project/spa/static/spa",
    "heroku-postbuild": "python ./manage.py collectstatic --noinput"
  }
}
